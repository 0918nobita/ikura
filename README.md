# ikura

git リポジトリ内のファイルを、累計変更量の降順にソートして出力します。

```text
Usage: ikura.exe [OPTIONS] [REPOSITORY_PATH]

Arguments:
  [REPOSITORY_PATH]

Options:
      --branch <BRANCH>
      --pathspec <PATHSPEC>
  -h, --help                 Print help
```
