# ikura

git リポジトリ内のファイルを、累計変更量の降順にソートして出力します。

```text
Usage: ikura.exe [OPTIONS] <REPO_PATH>

Arguments:
  <REPO_PATH>

Options:
      --branch <BRANCH>  [default: main]
  -h, --help             Print help
```
